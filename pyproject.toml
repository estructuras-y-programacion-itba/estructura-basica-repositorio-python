[project]
name = "estructura-basica-repositorio-python"
version = "0.1.0"
description = "Scaffold didáctico para proyectos de POO con Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "restaurante"
